"""trader — a risk-first, modular automated trading engine.

The same strategy + risk code runs in backtest, paper, and live; only the
data source and broker adapter swap behind an interface.
"""

__version__ = "0.1.0"
